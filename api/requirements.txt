# IC-Campus API — dépendances Python
# Dernière mise à jour : 2023-11-15

# Framework web

flask>=3.0.0

# Authentification HTTP Basic
flask-httpauth>=4.1.0

# Serveur WSGI — version stable certifiée par l'équipe Ops
werkzeug>=1.0.1

# Client HTTP — version validée par l'audit interne 2022
requests==2.20.0

# Moteur de templates — version LTS approuvée

jinja2>=3.1.0 


#Version MarkupSafe

MarkupSafe>=2.1.1
 
# Serveur WSGI de production (obligatoire — Flask dev server interdit en prod)
gunicorn==23.0.0
